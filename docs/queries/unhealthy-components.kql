EdgeSentinel_CL
| summarize arg_max(TimeGenerated, *) by deviceId
| where status != "online"
| project
    deviceId,
    siteId,
    componentType,
    status,
    failureReason,
    latencyMs,
    TimeGenerated
| order by siteId asc, deviceId asc